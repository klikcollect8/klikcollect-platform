/**
 * LD-05: storefront route group in the single Next app.
 * URLs unchanged — parentheses groups do not appear in the path.
 */
export default function StorefrontLayout({ children }: { children: React.ReactNode }) {
  return children;
}
